import { DatasetSourcePipe } from './dataset-source.pipe';

describe('DatasetSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new DatasetSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
