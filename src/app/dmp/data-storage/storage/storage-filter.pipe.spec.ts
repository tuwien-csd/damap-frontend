import { StorageFilterPipe } from './storage-filter.pipe';

describe('StorageFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StorageFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
